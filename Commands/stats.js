require('discord.js');
module.exports = {
	name: 'stats',
	description: 'Ping!',
    execute(message, args){
       message.reply("sorry this command is temporarly disabled Contact Rhys llewellyn #3527 for more info")
        //message.channel.send("silica's Information", client.user.displayAvatarURL({format: "png"}))
            
           
        
    }
}
